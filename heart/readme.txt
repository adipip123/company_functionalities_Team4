results from neural networks
73% accuracy in test set via neural networks(average)
100% mapping of training set(possible overfitting)


results from logistic regression
75% accuracy in test set
81% mapping of training set
faster in comparison to neural networks
